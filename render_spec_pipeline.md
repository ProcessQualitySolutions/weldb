# Render Specification — Pipeline (.weldp)

This document will define how WMDB Pipeline weld maps are rendered visually.

## Status

This render specification has not yet been defined. This file is a placeholder.

## Notes

The pipeline rendering rules (icons, cell types, merging behavior, color policy) will be defined here once the pipeline drawing standard is established. Pipeline drawings may differ significantly from the boiler grid-based layout.
